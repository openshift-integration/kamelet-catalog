- route:
    from:
      uri: "kamelet:timer-source"
      parameters:
        period: 1000
        message: "{ \"foo\": \"John\"}"
    steps:
      - to:
          uri: "kamelet:simple-filter-action"
          parameters:
            expression: "The Simple Expression"
      - to:
          uri: "log:info"
