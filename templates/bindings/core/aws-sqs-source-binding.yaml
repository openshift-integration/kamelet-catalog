- route:
    from:
      uri: "kamelet:aws-sqs-source"
      parameters:
        queueNameOrArn: "The Queue Name"
        region: "The AWS Region"
    steps:
      - to:
          uri: "log:info"
    